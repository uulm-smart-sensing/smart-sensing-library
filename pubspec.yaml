name: smart_sensing_library
description: A smart sensing library for study apps
version: 0.0.0
homepage:

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sensing_plugin:
    path: ./sensing-plugin


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test_cov_console: ^0.2.2
  junitreport: ^2.0.2
  cobertura: ^1.0.3

flutter:
